<?php
/* 
 * Generated by CRUDigniter v3.2 
 * www.crudigniter.com
 */
 
class Faixa_renda_familiar extends CI_Controller{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Faixa_renda_familiar_model');
    } 

    /*
     * Listing of faixa_renda_familiar
     */
    function index()
    {
        $data['faixa_renda_familiar'] = $this->Faixa_renda_familiar_model->get_all_faixa_renda_familiar();
        
        $data['_view'] = 'faixa_renda_familiar/index';
        $this->load->view('layouts/main',$data);
    }

    /*
     * Adding a new faixa_renda_familiar
     */
    function add()
    {   
        if(isset($_POST) && count($_POST) > 0)     
        {   
            $params = array(
				'descricao' => $this->input->post('descricao'),
            );
            
            $faixa_renda_familiar_id = $this->Faixa_renda_familiar_model->add_faixa_renda_familiar($params);
            redirect('faixa_renda_familiar/index');
        }
        else
        {            
            $data['_view'] = 'faixa_renda_familiar/add';
            $this->load->view('layouts/main',$data);
        }
    }  

    /*
     * Editing a faixa_renda_familiar
     */
    function edit($id)
    {   
        // check if the faixa_renda_familiar exists before trying to edit it
        $data['faixa_renda_familiar'] = $this->Faixa_renda_familiar_model->get_faixa_renda_familiar($id);
        
        if(isset($data['faixa_renda_familiar']['id']))
        {
            if(isset($_POST) && count($_POST) > 0)     
            {   
                $params = array(
					'descricao' => $this->input->post('descricao'),
                );

                $this->Faixa_renda_familiar_model->update_faixa_renda_familiar($id,$params);            
                redirect('faixa_renda_familiar/index');
            }
            else
            {
                $data['_view'] = 'faixa_renda_familiar/edit';
                $this->load->view('layouts/main',$data);
            }
        }
        else
            show_error('The faixa_renda_familiar you are trying to edit does not exist.');
    } 

    /*
     * Deleting faixa_renda_familiar
     */
    function remove($id)
    {
        $faixa_renda_familiar = $this->Faixa_renda_familiar_model->get_faixa_renda_familiar($id);

        // check if the faixa_renda_familiar exists before trying to delete it
        if(isset($faixa_renda_familiar['id']))
        {
            $this->Faixa_renda_familiar_model->delete_faixa_renda_familiar($id);
            redirect('faixa_renda_familiar/index');
        }
        else
            show_error('The faixa_renda_familiar you are trying to delete does not exist.');
    }
    
}
